<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 750 60" width="100%" height="60">
  <defs>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Share+Tech+Mono&amp;display=swap');

      .legend-text {
        font-family: 'Share Tech Mono', monospace, sans-serif;
        font-size: 20px;
        font-weight: 700;
        letter-spacing: 4px;
        text-transform: uppercase;
        fill: #F8FAFC;
        animation: legendGlow 3s infinite ease-in-out;
      }

      .legend-border {
        stroke: url(#cyan-purple-grad);
        stroke-width: 2;
        fill: none;
        rx: 12;
        animation: borderPulse 3s infinite ease-in-out;
      }

      .star-icon {
        fill: #38BDF8;
        filter: drop-shadow(0 0 8px rgba(56, 189, 248, 0.9));
        animation: starGlow 3s infinite ease-in-out;
      }

      @keyframes legendGlow {
        0% {
          fill: #94A3B8;
          filter: drop-shadow(0 0 2px rgba(56, 189, 248, 0.3));
        }
        50% {
          fill: #38BDF8;
          filter: drop-shadow(0 0 10px rgba(56, 189, 248, 0.9)) drop-shadow(0 0 20px rgba(139, 92, 246, 0.7));
        }
        100% {
          fill: #94A3B8;
          filter: drop-shadow(0 0 2px rgba(56, 189, 248, 0.3));
        }
      }

      @keyframes starGlow {
        0%, 100% { opacity: 0.5; transform: scale(0.9); }
        50% { opacity: 1; transform: scale(1.2); fill: #F43F5E; }
      }

      @keyframes borderPulse {
        0%, 100% { opacity: 0.5; }
        50% { opacity: 1; }
      }
    </style>

    <linearGradient id="cyan-purple-grad" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#38BDF8" />
      <stop offset="50%" stop-color="#8B5CF6" />
      <stop offset="100%" stop-color="#EC4899" />
    </linearGradient>

    <!-- Vector Star Path -->
    <g id="star">
      <path d="M 0,-8 L 2.4,-2.4 L 8,-2.4 L 3.5,1.2 L 5.2,7 L 0,3.3 L -5.2,7 L -3.5,1.2 L -8,-2.4 L -2.4,-2.4 Z" />
    </g>
  </defs>

  <rect x="5" y="5" width="740" height="50" class="legend-border" />
  
  <!-- Left Star -->
  <use href="#star" x="85" y="30" class="star-icon" />

  <!-- Text -->
  <text x="50%" y="50%" dominant-baseline="middle" text-anchor="middle" class="legend-text">YOU ARE WITNESSING THE STORY OF A LEGEND</text>

  <!-- Right Star -->
  <use href="#star" x="665" y="30" class="star-icon" />
</svg>
